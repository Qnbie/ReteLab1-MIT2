package hu.bme.mit.yakindu.analysis.workhere;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.List;

import org.eclipse.emf.common.util.EList;
import org.eclipse.emf.common.util.TreeIterator;
import org.eclipse.emf.common.util.URI;
import org.eclipse.emf.ecore.EObject;
import org.eclipse.emf.ecore.resource.Resource;
import org.eclipse.emf.ecore.resource.impl.ResourceSetImpl;
import org.eclipse.emf.validation.internal.modeled.model.validation.Event;
import org.junit.Test;
import org.yakindu.sct.model.sgraph.Reaction;
import org.yakindu.sct.model.sgraph.State;
import org.yakindu.sct.model.sgraph.Transition;
import org.yakindu.sct.model.stext.stext.impl.EventDefinitionImpl;
import org.yakindu.sct.model.stext.stext.impl.VariableDefinitionImpl;
import org.yakindu.sct.model.sgraph.Statechart;

import hu.bme.mit.model2gml.Model2GML;
import hu.bme.mit.yakindu.analysis.RuntimeService;
import hu.bme.mit.yakindu.analysis.TimerService;
import hu.bme.mit.yakindu.analysis.example.ExampleStatemachine;
import hu.bme.mit.yakindu.analysis.example.IExampleStatemachine;
import hu.bme.mit.yakindu.analysis.modelmanager.ModelManager;

public class Main {
	@Test
	public void test() {
		main(new String[0]);
	}
	
	public static void main(String[] args) {
		ModelManager manager = new ModelManager();
		Model2GML model2gml = new Model2GML();
		
		// Loading model
		EObject root = manager.loadModel("model_input/example.sct");
		
		// Reading model
		List<State> trap = new ArrayList<State>();
		List<String> events = new ArrayList<String>();
		List<String> variables = new ArrayList<String>();
		int nameCnt = 1;
		Statechart s = (Statechart) root;
		TreeIterator<EObject> iterator = s.eAllContents();
		while (iterator.hasNext()) {
			EObject content = iterator.next();
			if(content instanceof State) {
				State state = (State) content;
				if(state.getName() == null || state.getName()=="") {
					state.setName("State_" + nameCnt);
					nameCnt++;
				}
				System.out.println(state.getName());
				EList<Transition> transitions = state.getOutgoingTransitions();
				if(transitions.size() == 0) {
					trap.add(state);
				} else {
					for(int i =0; i < transitions.size();i++) {
						System.out.println(transitions.get(i).getSource().getName() + "->" + transitions.get(i).getTarget().getName());
					}
				}
			} else if(content instanceof EventDefinitionImpl) {
				EventDefinitionImpl edi = (EventDefinitionImpl) content;
				System.out.println(edi.getName());
				events.add(edi.getName());
			} else if(content instanceof VariableDefinitionImpl) {
				VariableDefinitionImpl vdi = (VariableDefinitionImpl) content;
				System.out.println(vdi.getName());
				variables.add(vdi.getName());
			}
		}
		System.out.println("Traps:");
		for(int i = 0; i < trap.size(); i++) {
			System.out.println(trap.get(i).getName());
		}
		
		System.out.println(printTemplate(variables));

		// Transforming the model into a graph representation
		String content = model2gml.transform(root);
		// and saving it
		manager.saveFile("model_output/graph.gml", content);
	}
	
	public static String runStatechartTemplate(List<String> events ,List<String> variables) {
		String result = "";
		
		result +="public class RunStatechart {/n/n";
			
		result +="\tpublic static void main(String[] args) throws IOException {\n";
		result +="\t\tExampleStatemachine s = new ExampleStatemachine();\n";
		result +="\t\ts.setTimer(new TimerService());\n";
		result +="\t\tRuntimeService.getInstance().registerStatemachine(s, 200);\n";
		result +="\t\ts.init();\n";
		result +="\t\ts.enter();\n";
		result +="\t\tString command = \"\";\n";
		result +="\t\tBufferedReader reader = new BufferedReader(new InputStreamReader(System.in));\n";
		result +="\t\tboolean run = true;\n";
		result +="\t\twhile(run) {\n";
		result +="\t\t\tcommand = reader.readLine();\n";
		result +="\t\t\tswitch(command) {\n";
		for(int i=0; i<events.size();i++) {
			result += "\t\t\tcase \"" + events.get(i) + "\":\n";
			result += "\t\t\t\ts.raise" + events.get(i).toUpperCase().charAt(0) + variables.get(i).substring(1) + "();\n";
			result += "\t\t\t\ts.runCycle();\n";
			result += "\t\t\t\tprint(s);\n";
			result += "\t\t\tbreak;\n";
		}
		result +="\t\t\tcase \"exit\":\n";
		result +="\t\t\t\trun = false;\n";
		result +="\t\t\t\tprint(s);\n";
		result +="\t\t\t\tbreak;\n";
		result +="\t\t\tdefault:\n";
		result +="\t\t\t\tSystem.out.println(\"Please give a valid command\");\n";
		result +="\t\t\t\t}\n";
		result +="\t\t\t}\n";
		result +="\t\treader.close();\n";
		result +="\t\tSystem.exit(0);\n";
		result +="\t}\n";
		result += printTemplate(variables);
		result +="}\n";
		
		return result;
	}
	
	public static String printTemplate(List<String> variables) {
		String result = "";
		result += "public static void print(IExampleStatemachine s) {\n";
		for(int i = 0; i<variables.size();i++) {
			result += "\tSystem.out.println(\"" + variables.get(i).toUpperCase().charAt(0) + " = \" + s.getSCInterface().get" 
					+ variables.get(i).toUpperCase().charAt(0) + variables.get(i).substring(1) + "());\n";
		}
		result += "}\n";
		return result;
	}

}
